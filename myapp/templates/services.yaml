apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.env }}
  name: {{ .Values.appname }}
spec:
  selector:
    app: {{ .Values.app }}
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
